# 交付验证

验证日期：2026-09-14。环境：macOS、Python 3.12.14、Node.js 24.19.0，浏览器检查使用独立临时配置的 Chrome。

## 实际网络抓取

直接执行 `scripts/fetch_nodes.py`，不是手工填写结果。初始 `site/data.json` 的检测时间为 **2026-09-14 09:29:08（北京时间）**。

| 项目 | GitHub | GitLab |
| --- | --- | --- |
| 抓取状态 | ok | ok |
| 网页编辑时间（北京时间） | 2026-09-04 07:12:27 | 2026-09-04 07:13:47 |
| 作者更新时间（北京时间） | 2026-09-04 07:30 | 2026-09-04 07:30 |
| 节点1 | VMess，10项参数 | VMess，10项参数 |
| 节点2 | AnyTLS，7项参数 | AnyTLS，7项参数 |

节点1与节点2的参数和导入配置均一致，选中 GitHub。作者更新时间原文在两站措辞略有不同，分别保留在 JSON 与页面中。

## 自动化检查

- **24项 Python 单元测试全部通过**：结构解析、标题和换行变化、动态地址、非法链接、缺失或重复内容、更新时间解析、仅日期的精度处理、语义比较、单站失败、时间接口失败、双站失败、异常快照、跨运行恢复及来源选择。
- `site/app.js` 与 `site/sw.js` 通过 Node.js 语法检查。
- `.github/workflows/update-and-deploy.yml` 通过 Ruby YAML 解析，包含 `build` 和 `deploy` 两个作业。
- 实际来源解析与模拟故障测试使用不同数据；故障测试没有覆盖初始真实快照。

## 浏览器检查

通过本机 HTTP 服务在 `/site/` 子路径访问，覆盖与 GitHub Pages 仓库路径相同的相对路径场景。

- 750px 固定设计宽度与1280px桌面居中布局，无横向溢出；已查看完整页面截图。
- 直接使用 Chrome 打开本地 `file://.../site/index.html` 时，内置预览数据可正常生成两个来源和四张节点卡片。
- 两个来源、四张节点卡片与一致性结果正常呈现。
- 顶部“复制节点1”“复制节点2”分别写入对应的单个导入链接，不会把两个节点合并到剪贴板。
- 来源卡片内的单节点复制与完整参数复制正常。
- 浏览器拒绝剪贴板权限时，手动复制窗口正确打开。
- 刷新结果后，已展开的参数区域保持展开。
- Service worker 成功控制页面，离线后重新加载仍可显示节点与缓存提示。
- Service worker 缓存内不含 `data.json`。
- 模拟两站失败后，显示旧数据标记，复制按钮改为“复制上次成功节点”。
- 首次加载且数据不可用时，显示空状态，复制按钮禁用。
- 上述过程无 JavaScript 页面错误。

## 验证范围

已完成本地功能验证和真实公开页面抓取。尚未上传用户的 GitHub 仓库，因此没有声称已验证真实的 GitHub Actions 调度或 Pages 线上部署；部署需按 README 在用户仓库启用 Pages 后执行。未测试节点服务器的实际连接速度或可用性，也未在实体 iPhone/安卓设备上测试安装流程。
